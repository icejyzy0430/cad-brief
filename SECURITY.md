# 安全策略

## 支持版本

安全修复应用于 `cad-brief` 的最新发布版本。

## 威胁模型

`cad-brief` 可能读取用户附件和公开网页资料。这些输入属于不可信证据，
可能包含提示注入、恶意文档行为、欺骗性元数据、敏感信息或指向可执行内容
的链接。

Skill 的安全边界包括：

- 忽略证据中嵌入的任务指令；
- 不执行下载的代码、脚本、宏、安装器或活动文档操作；
- 未经明确授权，不上传用户的私人文件或文件内容；
- 不泄露凭据、本机路径、项目代号和无关个人信息；
- 优先引用公开来源，而不是重新分发大量受版权保护的内容；
- 无法安全取得证据时降低 readiness，不绕过限制。

内置 validator 只使用 Python 标准库解析本地 Markdown。它不会访问 URL、
执行附件、检查 CAD 几何或证明研究事实真实。

## 报告安全问题

如果仓库已启用 GitHub Private Vulnerability Reporting，请通过私有
Security Advisory 提交，并包含：

- 受影响的发布版本或 commit；
- 最小复现步骤；
- 预期行为和实际行为；
- 是否涉及私人数据、命令执行或不安全的 TTC handoff。

不要在公开 issue 中提交真实凭据、专有 CAD 或用户私人文档。
